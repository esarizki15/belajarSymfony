{% extends 'base.html.twig' %}
{# {% extends '@AdminLTE/layout/default-layout.html.twig' %} #}
{% block title %}Tes{% endblock %}

{% block body %}
{# {% block page_content %} #}
<style>
    .example-wrapper { margin: 1em auto; max-width: 800px; width: 95%; font: 18px/1.5 sans-serif; }
    .example-wrapper code { background: #F5F5F5; padding: 2px 6px; }
</style>
<div class="container">
	<h1>User Form :</h1>
	{{form_start(user_form)}}
		{# {{form_widget(user_form.fullname)}} #}
		{# {{form_label(user_form.task, 'Nama Lengkap')}} #}
	{{form_end(user_form)}}
	<h1>Data Post</h1>
	<table class="table">
		<thead>
			<tr>
				<td>Judul</td>
				<td>Deskripsi</td>
			</tr>
		</thead>
		<tbody>
			{% for data in post %}
				<tr>
					<td>{{data.title}}</td>
					<td>{{data.description}}</td>
				</tr>
			{% endfor %}
		</tbody>
	</table>
</div>
{% endblock %}
